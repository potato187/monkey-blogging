import React from "react";
import PropTypes from "prop-types";

const index = ({ user, ...rest }) => {
	return (
		<div>
			<div>image user</div>
			<div>toggle</div>
		</div>
	);
};

index.propTypes = {};

export default index;
